with-expecter: True
packages:
  github.com/zagvozdeen/examly/internal/store:
    config:
      all: True
      filename:  "mock_{{.InterfaceName | snakecase}}.go"
      dir: "{{.InterfaceDir}}"
      outpkg: "{{.PackageName}}"
      inpackage: True